---
import FilterSelect from "@/components/app/ui/FilterSelect.tsx";

export interface FilterLink {
  value: string;
  label: string;
  href: string;
}

export interface Props {
  filters: FilterLink[];
  active: string;
}

const { filters, active } = Astro.props;
---

<FilterSelect filters={filters} active={active} client:load />