---
import ConfirmDelete from "@/components/app/ui/ConfirmDelete.tsx";

export interface Props {
  module: string;
  label?: string;
}

const { module, label = "elemento" } = Astro.props;
---

<ConfirmDelete client:load module={module} label={label} />
